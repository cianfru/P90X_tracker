/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Build timestamp injected at build time (see vite.config.ts `define`). */
declare const __BUILD_TIME__: string
